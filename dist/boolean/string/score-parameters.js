export default function ScoreParameters(score, valid) {
    if (valid) {
        return `score is compatible with Score interface`;
    }
    else {
        return `score is not compatible with Score interface`;
    }
}
//# sourceMappingURL=score-parameters.js.map